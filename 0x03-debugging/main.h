#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>

void positive_or_negative(int i);
int largest_num(int a, int b, int c);
void print_rem_days(int month, int day, int year);
int convert_day(int month, int day);

#endif

